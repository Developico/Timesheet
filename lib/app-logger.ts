// Unified logger — re-export for backward compatibility
export { appLog, withCorrelation, createLogger, sanitizeError } from './logger'
export type { LogData as LogDataBase, Level, Logger } from './logger'
